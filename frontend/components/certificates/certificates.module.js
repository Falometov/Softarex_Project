import certificatesComponent from './certificates.component';

const module = angular.module('frontend.components.certificates', []);

module.component('certificates', certificatesComponent);

export default module.name;
